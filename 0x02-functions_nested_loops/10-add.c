#include "main.h"
/**
  * add - a function that adds two integers and returns the result.
  * @a: first integer inputs to be added
  * @b: second integer inputs to be added
  * Return: there sum
  */
int add(int a, int b)
{
	int sum;

	sum = a + b;
	return (sum);
}
