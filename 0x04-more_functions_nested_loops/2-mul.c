#include <stdio.h>
#include "main.h"
/**
  * mul - a function that multiplies two integers.
  * @a: integer variable
  * @b: integer variable
  * Return: the multiplied value
  */
int mul(int a, int b)
{
	int m = a * b;

	return (m);
}
